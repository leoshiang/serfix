{
  "name": "serfix",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "pack": "pkg index.js -t  node18-win-x64 -o serfix.exe"
  },
  "private": true,
  "dependencies": {
    "csv-parser": "^3.2.0"
  }
}
